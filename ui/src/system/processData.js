export function processData(
    event,
    eventName,
    txHash,
    currentAdventurer
) {
   // const gameData = new GameData();
    switch (eventName) {
        case "StartGame":
            break
    }
}